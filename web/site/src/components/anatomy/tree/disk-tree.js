// disk-tree.js — GENERATED by tools/extract-tree-data.mjs. Do not hand-edit.
// Every code string is real, verbatim cabinet CSS from kiln's emitCSS()
// against a tiny synthetic cart, round-trip-verified at generation time.
// This module is the section's SKELETON; heavy folded nodes carry
// lazy: { ref, count } and their children live in paged JSON chunks
// under /anatomy/disk/ (see the tool header for the format).
// Regenerate: node tools/extract-tree-data.mjs disk

export const DISK_TREE = [
  {
    kind: 'section',
    label: "DISK",
    code: `/* ===== DISK ===== */`,
    folded: true,
    boxed: true,
    children: [
    {
      kind: 'decl',
      code: `@function --readDiskByte(--idx <integer>) returns <integer> {`,
      trailer: `}`,
      folded: true,
      children: [
      { kind: 'decl', code: `result:`, folded: true, lazy: {"ref":"disk/000","count":1} },
      ],
    },
    ],
  },
];

// Real measured size of this region in the synthetic cabinet.
export const DISK_TREE_META = { bytes: 1887 };
