// memr-tree.js — GENERATED by tools/extract-tree-data.mjs. Do not hand-edit.
// Every code string is real, verbatim cabinet CSS from kiln's emitCSS()
// against a tiny synthetic cart, round-trip-verified at generation time.
// This module is the section's SKELETON; heavy folded nodes carry
// lazy: { ref, count } and their children live in paged JSON chunks
// under /anatomy/memr/ (see the tool header for the format).
// Regenerate: node tools/extract-tree-data.mjs memr

export const MEMR_TREE = [
  {
    kind: 'section',
    label: "MEMORY READ",
    code: `/* ===== MEMORY READ ===== */`,
    folded: true,
    boxed: true,
    children: [
    {
      kind: 'decl',
      code: `@function --readMem(--at <integer>) returns <integer> {`,
      trailer: `}`,
      folded: true,
      children: [
      {
        kind: 'decl',
        code: `result:`,
        folded: true,
        children: [
        { kind: 'if', code: `if(`, trailer: `);`, lazy: {"ref":"memr/000","count":2053} },
        ],
      },
      ],
    },
    ],
  },
];

// Real measured size of this region in the synthetic cabinet.
export const MEMR_TREE_META = { bytes: 149816 };
