# CSS-DOS Logbook

Chronological work entries. Newest first. The durable handbook
(current state, sentinels, gotchas, how to test) is in
[`STATUS.md`](STATUS.md).

Last updated: 2026-07-12

Entries are individual files in [`entries/`](entries/). This page is
the **index only** — read the tag + one-line summary, open the 1–3
entries that look relevant. Newest first.

**Tags:** `LANDED` (shipped, on main/master) · `BRANCH` (exists only
on an unmerged branch — NOT in main) · `DEAD` (tried, reverted —
only open if retrying the same idea) · `FINDING` (a measurement
still true) · `PLAN` (the plan file is the live doc) ·
`SUPERSEDED` (later work invalidated this).

**Do not append full entries to this file.** New work → a new file
in `entries/` (`YYYY-MM-DD-slug.md`, ≤~15 lines) + one row here.
See [`PROTOCOL.md`](PROTOCOL.md).

| Date | Tag | Entry |
|------|-----|-------|
| 2026-07-12 | LANDED | [File map honest whole-tree reorg: Kiln emit reordered into ten coherent subsystem sections (declaration-inert — same 2.71M `--name:value` decls before/after, sha-fingerprinted), each subsystem's @property decls homed with its logic (registers→CPU, chip latches→their chip, kbd wires→keyboard selectors, write-slots→memory writes; "Memory declarations" now cells-only), CPU split into six sibling stages numbered where sequential, flag arithmetic moved out of shared helpers into CPU, stale `--incFlags8` dup deleted; bug fixes: disk "(N more…)" showed zero arms (page could strand a lone comment — fixed in writePages) and stacked show-more buttons (one shared reveal cursor now → ≤1 button)](entries/2026-07-12-filemap-whole-tree-reorg.md) |
| 2026-07-11 | LANDED | [Tree dedup + landmarks (owner round 3): CPU pane shows its real file region (borrowed flag-fn/register boxes deleted), decl pane retitled "Variable declarations", kiln gap comments mark address jumps in all six memory-scale lists (A/B equivalence PASS), region banner notes dropped, weighted pagination/`remaining` (runs count rows — "(7 more…)" lie fixed, no rows render past a pending more-button); anatomy data 15→7.5 MB](entries/2026-07-11-tree-dedup-gap-comments.md) |
| 2026-07-11 | LANDED | [Trees ship ALL the cabinet's code: caps/notes deleted; uniform stretches compress losslessly into periodic template+column `run` nodes (every row verified byte-exact at generation, expanded on demand client-side); memw 171 MB → 3.5 KB of linear runs, anatomy data 15 MB total, memr pages to its true 736k-row end](entries/2026-07-11-tree-full-content-runs.md) |
| 2026-07-11 | LANDED | [Trees extract from the REAL sokoban cabinet (headers show true region sizes; per-run caps ship heads + plain "… N more rows" notes; parser hardened for 171 MB regions); exhibit notes deleted; headings flattened to bare descriptions CPU-style ("palette function", "tick derivation", "store keyframe")](entries/2026-07-11-tree-real-cart-plain-headings.md) |
| 2026-07-11 | LANDED | [Chipset divided BY CHIP (PIT/keyboard/PIC/DAC, wires-then-registers each; emitIRQCompute split into emitKeyboardWires + emitIRQArbitration; declaration reorder proven inert by A/B set-compare); screen region gains palette/pixel sub-banners; principle 9 sharpened: divide by real structure first, then label — never label the lump](entries/2026-07-11-chipset-per-chip-groups.md) |
| 2026-07-11 | LANDED | [Tree "considered" pass (owner feedback on live trees): every decl/one-liner rule parses structurally (pixel & keyboard rows get indentation + run uniformity), single-group regions hoist + lone wrappers unfold (panes open onto content, ≤1 chunk on load), kiln banners consolidated (css-lib micro-banners, chipset "(preamble)" killed, clock constructs named), measured line budget fixes phone wraps, glyph pinned to top, constant tree header](entries/2026-07-11-tree-considered-pass.md) |
| 2026-07-11 | LANDED | [Anatomy Tree View on all 10 file panes with lazy chunked data (skeletons ~19 KB bundled, paged JSON fetched on expand, honest totals + explicit pixel-run cap; site bundle 740→235 KB); generic region parser replaces per-section recipes; kiln memory-region run-delimiter comments planted (equivalence-checked); Playwright browse test + websmoke PASS — native calcite gates still owed (env had no calcite)](entries/2026-07-11-anatomy-trees-all-sections-lazy.md) |
| 2026-07-11 | FINDING | [Suppressed (-1) width-2 memory writes corrupt byte 0 under spec-CSS semantics (Chromium 141 probe: applySlot hiOff=0 arm fires) but NOT under calcite — latent Chrome-vs-calcite divergence in every shipping cabinet (reg-form OR/AND/XOR/MOV16, INTO, FF jmp-reg); fix needs a calcite-capable session](entries/2026-07-11-applyslot-minus1-divergence.md) |
| 2026-07-11 | LANDED | [Kiln .cpu commenting-consistency pass: write slots regrouped by destination kind with run-delimiter comments + slot/gate sub-banners, opcode-family comments in long dispatches, per-row gate comments; old-vs-new equivalence proved (arm order only); websmoke PASS — native calcite gates still owed (env had no calcite)](entries/2026-07-11-kiln-cpu-commenting-pass.md) |
| 2026-07-11 | LANDED | [File Map carousel nav redesign: flanking arrows removed — wizard Back/Next walk the 11 sections (arrow keys inherit), Skip »»» from page 2+, hint re-dressed as yellow HINT note (dither spotlight deleted), shows on every carousel page, dismissal persists via localStorage](entries/2026-07-11-filemap-nav-redesign.md) |
| 2026-07-11 | LANDED | [Tree View round 3: run-uniformity policy (same-shaped siblings wrap together + fold together, folds 441→1361) and visual polish (depth shown exactly once — block tint/continuation tint/inline chip; right-edge comment column with wrap-reverse stacking; rigid conditions; hanging value wraps; cyan interactables; header icon+KB)](entries/2026-07-11-tree-uniform-runs-visual.md) |
| 2026-07-11 | LANDED | [Tree View round 2: .cpu rule regrouped IN KILN (two banner levels, aliases under REGISTERS — functionality-inert, smoke/writable/msdos PASS); tree mirrors it; long lists render in comment-delimited runs (each paginates independently — proven on --unknownOp's pre-else comment); comments-in-values verified safe (servo cssparser strips at tokenize); conventions doc: docs/plans/2026-07-10-anatomy-tree-view.md](entries/2026-07-11-tree-kiln-banners-runs.md) |
| 2026-07-10 | LANDED | [Site owner fix round: Home-hero h1 nowrap caused mobile sideways scroll (wraps ≤640px); inactive step-strip tabs get a darker #666 dither (all tabs dithered now); menu/status bars un-dithered to flat grey; Home + Why copy synced from `CSS-DOS-site-copy.md`; Why CTA buttons centred](entries/2026-07-10-site-owner-fix-round.md) |
| 2026-07-10 | LANDED | [Anatomy Tree View (CPU pilot): AST-powered tree above SectionCpu.svelte's prose — tools/extract-tree-data.mjs parses real emitCSS() output token-by-token, round-trip-verified, file order preserved (36 flag @functions → .cpu rule → @property blocks); fold points carved in data, one TreeAst renderer; owner-tested live](entries/2026-07-10-anatomy-tree-view-cpu-pilot.md) |
| 2026-07-10 | LANDED | [Machine element renamed `.cpu` → `.motherboard` (one element, both classes); kiln splits a new CHIPSET rule (PIT/PIC/kbd/DAC, 16 KB) out of the CPU rule (now honestly decode + registers, 265 KB); file reordered util→cpu→chipset→keys→screen→decl→memr→memw→disk→clock with clock contiguous; nested `&.cpu` rejected — calcite skips non-`:has` nested rules; site gets a Chipset carousel page + re-measured map; sokoban boots to tick 3M on calcite](entries/2026-07-10-motherboard-chipset-split.md) |
| 2026-07-10 | LANDED | [Site copy: CSS-lens additions to `docs/CSS-DOS-site-copy.md` — decoder (parallel fetch + prefix re-aliasing), REP IP-moonwalk, standing-decode vs in-branch laziness, idle-branch hardware defaults, keyboard edge derivation + hold-wire pigeonhole queue, DAC sub-index protocol, out-of-1MB shadows + sparse-memory-as-omission, clock-as-invalidation](entries/2026-07-10-site-copy-css-lens-additions.md) |
| 2026-07-09 | LANDED | [Removed the CPU register debug readout (`.cpu::after` counter display, `emitDebugDisplay` in kiln/template.mjs) — leftover debug scaffolding, superseded by the calcite debugger. Smoke 6/6 PASS; also updated CABINET-ANATOMY.md §7 and the site's SectionKeys.svelte anatomy walkthrough, which had documented it as a feature](entries/2026-07-09-remove-debug-readout.md) |
| 2026-07-09 | FINDING | [Chrome vs a 316 MB cabinet, measured (new `web/player/experiments/huge-css-test.html`): `<link>` parse ~6 s no crash (V8 ~536 MB cap is fetch-path-only); first style resolution of one `.cpu` div ~310 s then completes with correct tick-0 registers; with a live clock the tab wedges forever (recalc ≫ clock period — the "crash" is an unbounded recalc backlog). **BUG found + FIXED:** raw.html's same-element `class="clock cpu"` let `.cpu`'s animation shorthand cascade-clobber `anim-play` → the raw player's machine was frozen at tick 0 by construction; raw-regen now nests `.clock` (on `.window`) over `.cpu` (on `.window-body`), e2e-verified ticking in Chromium (hello-text: cycleCount/IP advance)](entries/2026-07-09-chrome-eval-huge-cabinet.md) |
| 2026-07-09 | LANDED | [EGA cyan "click me" pass: `.btn.primary` (Next/CTA/Build buttons), step-strip clickable tabs, and the StepDots selected-dot fill switch from navy/red to `var(--edit-cyan)` with black text (not `--edit-blue-bright`, which stays the desktop-weave colour); current step-strip tab gains TUI-style `‹ ›` angle brackets](entries/2026-07-09-cyan-clickme.md) |
| 2026-07-09 | LANDED | [Svelte CSS defragment: 15 single-owner `_fragments/*.css` port artifacts moved into their component's scoped `<style>` (foldable, moon-viz, tick-clock, ram-write, css-demo, term, step-dots, sign-demo, kbd-demo, file-map, pixel-screen, split-pane, result-floppy, build-progress, env-notice); `cart-grid.css` split across its 3 real owners (CartGrid/CartCard/CustomPanel). `cabinet-bar.css` kept, slimmed to the 2 selectors About.svelte renders as siblings of `<CabinetBar>`. Screenshot-verified pixel-identical across 7 routes incl. Foldable open/fold-bg/advanced-variant states](entries/2026-07-09-svelte-css-defragment.md) |
| 2026-07-07 | LANDED | [Corduroy 0.6.0: Shift/Ctrl/Alt are real modifiers — INT 09h was dropping breaks, never writing BDA 0x417, buffering modifier makes as bogus keys, one unshifted table (INT 16h consumers saw no modifiers ever); now flag-tracked + shift/ctrl translate tables (Ctrl+C = DOS break). FINDING: simultaneous `:active` presses can't chord (single-valued wire) — chords need the hold wire/multitouch; calcite-cli --press-events grew a `checked@sel` pseudo prefix (calcite `8928622`) to test it natively. msdos4-verified: hold→Shift,A → 'A', drain-alone clears flags, Ctrl+C → ^C. smoke/msdos/writable/kbd-e2e PASS](entries/2026-07-07-corduroy-modifier-keys.md) |
| 2026-07-07 | LANDED | [Site owner-litany: three sections again (About(7pg: Home hero + Why?-with-CTA-fork)/Build/Play; Play Next → "How it Works" → #about/how), per-cart `display.playTips` HINTS toast on Play (PoP/msdos4/doom tips), Build config: writable-disk checkbox + honest size warning (writable Sokoban measured 295→566 MB — over the V8 cap) + stacked option sets, compact green ✓ result row + non-wrapping «/» pager, Play mobile squish-to-fit iframe, dos-shell de-listed (cart kept), msdos4 `gfx:true` restores the BIOS splash (466.8 MB, gate PASS), SectionDisk "Writable disks" viewer section](entries/2026-07-07-site-three-sections-toasts-writable-ui.md) |
| 2026-07-07 | LANDED | [msdos4 "slow boot" was wasm compile, not boot ticks (boot = 1.45M ticks, faster than EDR-DOS): writable-shadow region-relative write keys fell off calcite's parse fast-path → 99s in wasm cssparser. Calcite `HoleKind::AddrOffset` fix: web compile 105.4s → 10.8s median, time-to-prompt ~18s. New bench profile `msdos-boot` (compile/run split + fetch/save timing + phaseReport); helps all writable carts](entries/2026-07-07-msdos4-compile-wall.md) |
| 2026-07-07 | LANDED | [Hold-off releases immediately + hold lamp + Alt key + SW heartbeat. Calcite `#kb-hold` becomes a submit key; the BRIDGE owns the mode and flips `--kbdHold` per press, so turning hold off drains held keys at once (was: only on the next key press — the page checkbox could only ride key submissions; `holdmode=` param deleted). Lamp dot on the key = `<img src=/_screen/holdlamp>`, a second SW multipart stream showing the machine's real wire state; raw player keeps the checkbox (raw-regen swaps the control). Alt (0x38) added between Ctrl and Space. **Big catch: the SW idle-dies in ~30 s without fetch events, permanently freezing the screen if the user just watched — fixed by a zero-JS self-refreshing heartbeat iframe.** Plus: multipart parts paint only as the next part streams in (delimiter-trailing + lamp double-emit), canvas readback of multipart imgs is stale (screenshot asserts). Smoke 6/6 + writable + msdos + kbd-e2e PASS](entries/2026-07-07-hold-toggle-immediate-lamp-alt.md) |
| 2026-07-07 | LANDED | [`npm run revendor`: scripted vendor sync (copy + `VENDOR-INFO.json` provenance + websmoke, fails if the bundle doesn't boot); dev servers + site prod build now WARN on sibling-vs-vendored engine hash mismatch](entries/2026-07-07-revendor-script.md) |
| 2026-07-07 | LANDED | [websmoke gate: new `run.mjs websmoke` + `web-boot.playwright.mjs` boot hello-text/dos-writable/msdos4 through the real web path against the **vendored** wasm — the only gate executing the engine the site ships. Negative-tested: old bundle FAILs with the exact `compile error: unreachable`; new bundle 3/3 PASS in ~93 s](entries/2026-07-07-websmoke-gate.md) |
| 2026-07-07 | LANDED | [Site msdos4 `compile error: unreachable` root-caused: vendored wasm (`web/vendor/calcite-pkg/`, cut 07-03) predates the 07-06 calcite writable-disk engine work — old engine panics compiling the writable cabinet; native gates can't see a stale bundle. Re-vendored from calcite `2f0d012`; A/B web e2e: old bundle reproduces, new boots MS-DOS 4.00 to `A>`; rom cabinet + native msdos gate green](entries/2026-07-07-site-msdos4-stale-vendored-wasm.md) |
| 2026-07-07 | LANDED | [Hold wire: machine-side multi-key hold (chords). `#kb-holdmode:checked` → `--kbdHold`; while up the machine suppresses key release edges, latching scancodes into 8 `kbdHeld*` slots (guest sees makes without breaks = chords), drained as IRQ-paced break codes on wire drop. Bridge latch machine + per-key `-hold` kiln rules deleted; both players share identical pure-CSS hold, raw-regen injects nothing (player-side toggle/lamp split off same day — top row). CLI typing regression, smoke 6/6, kbd-e2e doom chord LEFT+CTRL PASS](entries/2026-07-07-hold-wire-chords.md) |
| 2026-07-06 | LANDED | [Stage 3: real MS-DOS 4.00 boots (`carts/msdos4`, `boot.os`, Corduroy 0.5.0 INT 19h + INT 13h register contract, keyboard bridge 0x500→0x4F4, calcite rep-Copy PerIterFallback, gossamer IVT de-staled via 'IVTG' anchor); new `run.mjs msdos` gate](entries/2026-07-06-msdos4-boot-stage3.md) |
| 2026-07-06 | LANDED | [Session-writable disk (Stage 2): `disk.writable` shadow cells + Corduroy INT 13h AH=03h/04h + calcite windowed packed backing; Chrome ~6-sig-digit precision cliff FINDING (computed values >= 1e6 round) shaped the design; new `run.mjs writable` e2e](entries/2026-07-06-writable-disk-stage2.md) |
| 2026-07-06 | LANDED | [Play iframe fit-to-content fixes: 'load'-event race left the frame on the 700px fallback (huge gap under the keyboard) — measure on readyState too; parent-window ResizeObserver on a child-doc body never fires in Chrome — create it via contentWindow, per load. Blue shadow dropped; embed mode painted in the site's #d6d6d6 so the iframe blends into the host window](entries/2026-07-06-play-iframe-fit-fixes.md) |
| 2026-07-06 | LANDED | [Corduroy v0.3.0: digits + `,` `.` `/` were dead keys — INT 09h's scancode2ascii table (not the wire's ASCII byte) had 0x00 holes for 0x02–0x0B and 0x33–0x35; typing `edit readme.txt` dropped the dot. F1 was never broken (empty template on fresh boot). Every key class verified e2e on dos-shell (digits, punct, Esc/Bksp/Enter, F1/F3 recall, arrows in FreeDOS Edit); smoke 6/6](entries/2026-07-06-corduroy-key-table-digits-punct.md) |
| 2026-07-06 | LANDED | [dos-shell cart (9 FreeDOS utils, boots to A:\> — programs untested by agent, owner testing) + site cover-less text card (`display.bullets`/`accent`) + wide "load your own" bar; cart-format.md de-staled (boot.runCommand); stage 2/3 plan in plans/](entries/2026-07-06-dos-shell-cart-text-card.md) |
| 2026-07-06 | SUPERSEDED | [Player hold-key rework, zero-JS "design B": calcite = hold MODE (lit "Hold a key..." checkbox label rides submissions as holdmode=1, bridge latches/toggles; no key colour — one zero-JS click can't both flip page state and submit, and CSS url() side channels fire once per page, probed); raw = pure-CSS one-shot with red held key, machinery injected by raw-regen. Brief same-day shim (script in calcite.html) vetoed + removed. Single-key bridge latch could not chord — replaced next day by the machine-side hold wire (2026-07-07 row); the zero-JS findings (one click cannot both flip state and submit; CSS url() fires once/page) remain valid. All keys verified e2e on dos-shell incl. `.` and F1/F3](entries/2026-07-06-hold-a-key-rework.md) |
| 2026-07-06 | LANDED | [Site: four sections Home/Build/Play/About (About now after Play; Intro → Home step); Play iframe sized to real embed height, debug status line → quiet Change-program button, on-page cabinet gate (evicted cache bounces to #build); cart click advances to configure; upload pickers fixed for mobile (button.click(), no extension-only accept); How-it-works hub: white topper + flanking arrows + spotlight hint (Next un-gated). Gotcha: fixed descendants of sticky get layer-clipped — overlay must be a sibling](entries/2026-07-06-site-four-sections-play-polish.md) |
| 2026-07-06 | LANDED | [Player plumbing rebuilt: stateless SW + one cssdos-bridge BroadcastChannel (register/need-bridge handshakes deleted — an idle-killed SW needs no recovery); Cache Storage is the cabinet's only home (bridge compiles from cache on viewer-connect; rehydration = normal path), stored chunked in 64 MB parts because single ~330 MB puts die on Windows Chromium; /_stream/fb → /_screen/framebuffer. Chromium e2e + CDP stopAllWorkers + kbd-e2e doom PASS](entries/2026-07-06-stateless-sw-broadcast-bus.md) |
| 2026-07-06 | LANDED | [Boot splash held ~3 guest seconds (owner ask): PIT ch0 now armed at POST (18.2 Hz BDA tick finally runs from boot; INT 1Ah meaningful), splash paces on the real tick counter via STI + poll (HLT re-halts forever here — IRQ frame pushes current IP), never-rendered MEMORY POST line fixed (SS≠DS stack-buffer bug) + 12px line pitch. Boot ticks +~750K for every Corduroy cabinet — old boot-milestone tick constants stale. Smoke 6/6, kbd-e2e PASS](entries/2026-07-06-splash-hold-pit-post.md) |
| 2026-07-06 | LANDED | [Cabinet rehydration: builds survive page reloads. pagehide purge dropped (cabinet persists until next build); boot shim re-feeds the bridge from Cache Storage; build.restored state re-unlocks Play and replays a #play deep link. Closes the 2026-07-03 "builder-tab reload strands the player" trap — the need-bridge handshake restores the port, this restores the cabinet. Chromium-verified: reload on #play → auto-return, recompile, frames](entries/2026-07-06-cabinet-rehydration.md) |
| 2026-07-06 | LANDED | ["startRunning called while already running — no-op" stuck in the player status bar after every lazy build: viewer-connected's continuation re-called startRunning after compileCabinetBytes' viewerWaiting hook had already started the run. Lazy branch now returns after compile; guard message demoted postStatus→postDebug](entries/2026-07-06-startrunning-noop-fix.md) |
| 2026-07-06 | SUPERSEDED | [Second machine's convergent (uncommitted) SW hang fix superseded by 94e35dd; stashed + fast-forwarded. Unique rehydration piece integrated the same day (row above); stash dropped](entries/2026-07-06-sw-fix-convergence-stash.md) |
| 2026-07-05 | LANDED | [Hold-a-key: per-key pin checkboxes. Kiln emits `#kb-X-hold:checked` rules beside `:active`; player keyboard becomes a script-free GET form (keys = submit buttons, pins ride as `held=` params); bridge latches via the 'checked' pseudo-class (no calcite change). Works in raw CSS and calcite; pinned key highlighted. One held key at a time — no chords (single --keyboard wire). Pin-checkbox UI replaced by the arm-then-press gesture 2026-07-06, which itself was superseded by the 2026-07-07 hold wire; the per-key `-hold` kiln rules were deleted then — only the script-free GET-form keyboard survives](entries/2026-07-05-hold-key-pins.md) |
| 2026-07-05 | LANDED | [PoP ground-touch freeze fixed: OUT 0x43 ch2 speaker control word was zeroing PIT ch0 reload, killing IRQ 0; emitIO now decodes channel-select. PoP runs/jumps/falls past landings](entries/2026-07-05-pop-pit-ch2-freeze.md) |
| 2026-07-04 | LANDED | [Play hang-forever root-caused + fixed: SW idle-restart wipes bridgePort and nothing re-registered it — new cssdos-need-bridge recovery handshake (SW asks, boot shim re-hands a fresh port, waiting stream gets its viewer-connected). CDP-verified. Trap: web/site/sw.js was a dead duplicate of public/sw.js (deleted; public/ canonical). Also: player loading text 50-100MB→~300 MB; phone margins rethought, playing surface 304→368px at 390w](entries/2026-07-04-sw-idle-restart-hang.md) |
| 2026-07-04 | LANDED | [About/Play ironing pass (owner list): Calcite gets About page 4 + Play becomes player-first with a pure-CSS #embed player mode and a raw-CSS warning modal; Term underlines replace the glossaries + what-is-CSS fold (blue dotted restyle); full-ADD/jumps/conductor/latch/palette promoted out of folds; tippy first-visit hint bubble on the cabinet bar; callouts rebuilt with pixel icons (info/tip/warn). New deps: tippy.js, unplugin-icons, pixelarticons](entries/2026-07-04-about-ironing-pass.md) |
| 2026-07-04 | LANDED | [Six new verbatim code exhibits in the How-it-works sections, all verified against kiln emitters: CPU branchless-jump fold (JZ row + JL's XOR-as-arithmetic), keys debug read-out (registers as CSS counters), screen per-pixel rule + paletteRGB arm (6-bit DAC ×255/63; the file's only colour-returning function), disk window arm (LBA from cell 632), clock 1978 cycle price list, memr keyboard-bridge arms. Owner prose review pending](entries/2026-07-04-section-code-exhibits.md) |
| 2026-07-04 | LANDED | [About code blocks now highlighted by real Prism (bundled from npm, CDN scripts dropped) via new CodeCss component + lib/prism.js grammar extension (variable/number tokens); ~110 hand-written tok-* spans deleted from the anatomy sections, DOS-palette token theme. Rendered text verified byte-identical pre/post. Interactive widgets keep manual spans (live-bound code)](entries/2026-07-04-prism-code-blocks.md) |
| 2026-07-04 | LANDED | [Cabinet bar rework: sticky map header pinned over the carousel, one to-scale sliver + a ~350× zoom box holding all three too-small sections (was CPU-only), caption dropped for a title row tied to the lit segment by a coloured tick, duplicate pane header removed; router scroll-reset fixed to target `.wiz-scroll`. New inline `Term` component (dotted underline → definition card on hover/tap) seeded with cabinet/cart/register/tick/floppy/opcode/DOS](entries/2026-07-04-cabinet-bar-zoom-terms.md) |
| 2026-07-04 | LANDED | [COOP/COEP hard-gate dropped: health.svelte.js no longer blocks header-less hosts (GitHub Pages/mobile); false "host must send COOP/COEP" claims corrected in site README/vite/runtime-assets/STATUS. Headers still emitted (optional, future wasm-threads). e2e: headers stripped → no banner, build→play→frames](entries/2026-07-04-drop-coi-gate.md) |
| 2026-07-04 | FINDING | [COOP/COEP NOT required: no SAB/shared-memory/atomics anywhere in the live path (engine is single-threaded wasm, postMessage transport); full build→play verified in Chromium with headers stripped + SharedArrayBuffer undefined. The health gate added same-day over-blocks; README claim false](entries/2026-07-04-coop-coep-not-required.md) |
| 2026-07-04 | LANDED | [On-page health indicators: capability banner (SW/HTTPS/COOP+COEP/WASM/low-RAM reasons) on Build+Play via new health.svelte.js + EnvNotice, live bridge status line in the inline-player bar (red on boot/compile/engine errors) — mobile users finally see WHY it broke; also fixes the 10–25s silent-compile trap](entries/2026-07-04-onpage-health-indicators.md) |
| 2026-07-04 | LANDED | [Inline player on Play route: "Run using Calcite" now iframes calcite.html into the page (same-tab bridge) — fixes mobile Chrome freezing the backgrounded bridge tab in the old two-tab flow; Pop-out link keeps the old behaviour. e2e-verified headless (sokoban build → inline frames → Stop)](entries/2026-07-04-inline-player-mobile.md) |
| 2026-07-04 | LANDED | [About section critique pass: pages reordered (Why → epilogue), hub renamed "How it works" + map/overview landing page (GLOSSARY callout: cart/cabinet/tick) + header-comment section deleted, page 2 rebuilt as the mechanisms page (CssDemo absorbed as a background fold; year-and-a-half + Calcite mention promoted out of the last FAQ fold), FAQs deduped skeptic-first with section deep links + 3 new Qs (sound/playability/own programs), memw main flow rewritten to the ABOUT-SCRIPT register + verbatim per-cell applySlot cascade added, util sign() claim corrected (SUB/CMP borrow, verified vs kiln flags.mjs), carousel arrow-key wrap trap fixed. Playwright-verified, 0 console errors](entries/2026-07-04-about-critique-pass.md) |
| 2026-07-03 | FINDING | [Play-page calcite `unknown function: rgb` warning is benign (pixel painter's `--paletteRGB`, fires in every successful wasm compile ~3s in); flow verified end-to-end (Sokoban 295MB/13s, Doom 323MB/9.5s → title). Real traps: no compile feedback in the player tab for 10–25s, builder-tab reload (incl. Vite HMR) silently kills bridge+cabinet so the player waits forever (fixed 2026-07-06: cabinet rehydration), and leftover `[linear branch]` debug dumps in calcite compile.rs flood DevTools during compile](entries/2026-07-03-calcite-play-rgb-warn-benign.md) |
| 2026-07-02 | LANDED | [About section rebuilt as a 10-sub-page teaching sequence (basics reordered + new deep-dives: Inside the file with a to-scale 309MB map, One tick with a pure-CSS four-beat clock widget, Tricks with 9 lacks-X-so-Y blocks incl. a live sign() demo) with live widgets — div-grid screen, zero-JS `:active` keyboard, MOV-into-RAM recalc feed, verbatim-code CPU section (ADD arm, flag-by-flag addFlags16, DAA in full; 232 opcodes / 1,094 arms measured from a real build). Body text switched to More Perfect DOS VGA (−1px back onto the 8px grid), 8x16 pinned on chrome/code. Landed to master in the 2026-07-02 unification merge. Companion doc `CABINET-ANATOMY.md`](entries/2026-07-02-about-anatomy-subpages.md) |
| 2026-07-01 | LANDED | [Web Svelte site styling & UX pass (18 fixes): wizard chrome (dot spacing, min-height, de-bold h1s, monospace `<code>` chips), About intro gold scalloped banner + GitHub link + SvarDOS credit, Build cart-grid cap/no-shadow + flex configure page + non-blue spec column + visibly-grey disabled buttons, result-page redesign (small floppy header, one-line centred pager, tall source box), Play width constrained, shared `Foldable` component w/ blue `[+]` glyph + blue radio dot, dead-CSS audit. `vite build` green; landed to master in the 2026-07-02 unification merge](entries/2026-07-01-web-svelte-styling-pass.md) |
| 2026-07-01 | LANDED | [Website Svelte 5 port + static-hosting rebuild: `web/site/` is now a Svelte 5 (runes) app that `vite build`s to a plain static `dist/` runnable on any header-capable host (Vercel/Netlify/CF Pages), replacing the monolithic HTML+JS site and its bespoke Node dev server. Single `RUNTIME_COPIES` table (scripts/runtime-assets.mjs) drives dev-serve and build-copy of the runtime files the site fetches but Vite doesn't bundle (browser-builder ESM graph + kiln/builder/tools reach, presets, dos/bin, prebake, shim, player, vendored calcite WASM, carts); emits `carts/index.json` + `vercel.json`/`_headers` (COOP/COEP). browser-builder copied-not-bundled (Rollup-external; mkfat12 shebang stripped). `display.cover` in program.json opts a cart onto the landing grid. Deleted the 210-line dev-middleware + `/_status`/`/_reset`/`/_clear`; root `npm run dev`→Vite, `dev:legacy`→old server (still needed by kbd-e2e/compile-phase-capture harnesses). Verified on a static COOP/COEP host in Chromium: crossOriginIsolated, bridge+SW on deep-link, 323MB Doom cabinet built end-to-end. Old site files + old dev.mjs NOT yet deleted (would break the two old-DOM harnesses)](entries/2026-07-01-web-svelte-static-port.md) |
| 2026-07-01 | LANDED | [Build page: headings unbolded, intro sub-page gray lightened + logo un-bordered + centered + one-line title, play step's run-option cards stacked, Build step restructured into 3 sub-pages (pick/configure+build/cabinet ready) mirroring About's dot pattern — replaces the old 01-04 numbered blocks, adds a boot-mode radio (program vs. DOS shell) driving the now-hidden run-command field, gates Next with a tooltip via a `#start.disabled` MutationObserver, hides the redundant `#stages` checkbox-list progress display (small progress window only), relabels BIOS row "Firmware + OS". Along the way fixed a real mobile bug: the wizard's `body{display:grid}` host had no explicit column, so `max-width: calc(100% - 32px)` resolved against the dialog's own 820px instead of the viewport, causing horizontal overflow ≤820px. Playwright-verified desktop+390px, smoke 6/6](entries/2026-07-01-build-page-redesign.md) |
| 2026-06-30 | LANDED | [Wizard hash routing: the single-page landing/build wizard (About/Build/Play tabs were JS-toggled `.step` sections) now uses the URL hash as the route — `setStep()` writes `#about`/`#build`/`#play`, `applyHash()` restores it on load + hashchange (same gating: `#play` pre-build → `#build`), a `syncingHash` flag breaks the echo loop, `#games` kept as a `#build` alias. Refresh now keeps the step instead of resetting to About. No framework/build step (owner declined SvelteKit-static for the minimal fix). Playwright-verified, console clean](entries/2026-06-30-wizard-hash-routing.md) |
| 2026-06-30 | LANDED | [Build-page cart picker reworked: box art no longer cropped (`object-fit: cover→contain`, dropped `image-rendering: pixelated`), 3 covers swapped for clean full-box scans (doom-alt/pop-alt/zork-box-2), per-card name/desc text removed in favour of one `#cart-detail` box (name as header + blurb) below the grid, selection now a thick green border (was black fill, invisible against card borders), custom card carries its text inside a dashed muted-fill cover. Both selection sync paths in `wizard.js` updated; Playwright-verified in Chromium, console clean](entries/2026-06-30-build-page-boxart-cards.md) |
| 2026-06-30 | LANDED | [Raw player fixed + paintable: `raw.html` now derives from `calcite.html` (`raw-regen.mjs`) so chrome/keyboard match exactly, `<img>`→64,000-element CSS pixel grid under a `.window-body.clock.cpu` host, label `RAW`. New `kiln/pixels.mjs` (always emitted, inert in calcite path) paints each Mode 13h pixel from `--__1mc{cell}` via a 256-arm `@function --paletteRGB()` over the live DAC; 8×8 render proof passes in real Chromium 149 (CSS `@function`/`if()`/`style()` supported); smoke 6/6; painter +6.17 MB fixed; full 64k grid crashes headless Chrome — the "theoretical player" by design](entries/2026-06-30-raw-player-pixel-grid.md) |
| 2026-06-29 | BRANCH | [Sokoban cart made buildable (branch `cleanup/public-presentation`): `carts.js` advertised sokoban but `carts/sokoban/` had no `program.json`, so the public grid offered an unbuildable cart. `SKB.EXE` is genuinely Sokoban (verified via embedded strings); authored `program.json` (`cart@1`, `runCommand: SKB`, CGA `cgaGfx: true`); builds + fast-shoot @4M ticks reaches its title/system-select screen](entries/2026-06-29-sokoban-program-json.md) |
| 2026-06-29 | BRANCH | [Public-presentation cleanup sweep (branch `cleanup/public-presentation`, 7 commits): personal-path leaks scrubbed (2 usernames — NASM + Playwright fallbacks → env/PATH, `.mcp.json` relativised), dead/broken scripts deleted (fb-pack-diff, bit-rotted ref-muslin), stale cart refs fixed (carts.js, artifacts.mjs), noisy dev docs gitignored (`docs/benches/`) + archive cruft pruned, stale `transpiler/`→`kiln/` refs, web honesty (menu comments, VSYNC-PLAN refs, bridge canary), doc-vs-code contradictions (write slots 6→3, stages four→five, Corduroy default not "experimental"), dead code removed (6 emitters, ghost JSDoc, no-op flag); smoke 6/6](entries/2026-06-29-public-presentation-cleanup.md) |
| 2026-06-16 | FINDING | [Phase 0 settles the compiler road: Doom ingame is dispatch-bound NOT memory-bound (v2's excuse refuted) — hot-region work is 595× the interpreter even via hashmap, 11,564× in V8; interpret-vs-compile 849× ceiling / ~4.5× floor; v2 lost 45× from whole-tick DAG eval + per-read host crossings, both fixable; verdict GO, codegen the v1 branch-skipping op stream to flat-linear-memory wasm](entries/2026-06-16-phase0-compiler-road-dispatch-bound.md) |
| 2026-06-12 | LANDED | [Cabinet compile wall 30.0 → 4.6 s web (−85%, same-day driver A/B): calcite byte-level fast paths for data-as-code (literal dispatch runs, buffer-copy runs) + dispatch-table/function-branch deep copies eliminated; every step state-dump byte-identical at 2M ticks; new bridge `phase-report` + capture script expose the wasm compile breakdown](entries/2026-06-12-compile-wall-fastpath.md) |
| 2026-06-12 | LANDED | [Release audit: Corduroy INT 10h AH=1Ah dispatched DCC on BL not AL (PoP 1.4 "Graphics mode not available") — fixed, smoke PASS; pop1_4 + rogue1_0 verified booting (pop1_4 needs sound drivers .BAK'd — 7 kHz PIT ISR starves an 8086); calcite genericity sweep clean; 2026-04 seg-override POP report verified gone; dead plans + stale debugging docs pruned](entries/2026-06-12-release-audit-bios-dcc-fix-cart-verification.md) |
| 2026-06-12 | LANDED | [VGA webfont self-hosted + CC BY-SA attribution fixed: was hotlinking a third-party mirror with no credit (license violation); now served from web/player/fonts/ with license file, README provenance, visible site credit](entries/2026-06-12-vga-webfont-self-hosted-attribution.md) |
| 2026-06-12 | LANDED | [calcite column_drawer_fast_forward DELETED (calcite main 788389d, −308 lines): last x86-aware code block gone, audit found no other upstream knowledge in crates/ — cardinal rule holds tree-wide; build+tests+smoke 7/7](entries/2026-06-12-calcite-column-drawer-deleted-fully-generic.md) |
| 2026-06-12 | LANDED | [Player keyboard was DEAD since the 2026-05-28 merge: host stack (player/SW/bridge) kept the old `set_keyboard` model while calcite deleted that API; clicks threw into a swallowed catch. Pseudo-input host stack restored (`/_kbd?class=` → `kbd-active` → `set_pseudo_class_active` pulses, bridge v48); browser-builder now plumbs `disk.sectorsPerCluster`; new real-input e2e `web/tests/kbd-e2e.playwright.mjs` covers the bench's blind spot](entries/2026-06-12-player-keyboard-dead-pseudo-input-host-restored.md) |
| 2026-06-12 | LANDED | [Player full keyboard restored (Esc/F1–F10/Tab/Caps/Shift/Ctrl) — lost in the keyboard side-channel reverts, never re-merged; plus owner-reviewed resize: 640px screen+keyboard with uniform margin, rotated-► uniform arrows, ↵ Enter](entries/2026-06-12-player-full-keyboard-restored.md) |
| 2026-06-11 | LANDED | [doomLoad kernel characterised + fixed: EDR-DOS `fatptr` FAT chain walk was 21% of doomLoad (div32+getblk+locate_buffer chain ~41% total; map regenerated from edrdos rev 72ae65f, byte-verified vs guest); new `disk.sectorsPerCluster` cart option, doom8088 → 32 (16K clusters): doomLoad ticks 29.55M→9.25M (−68.7%), web doomLoad 59.6→19.1 s healthy-pair; smoke 7/7](entries/2026-06-11-doomload-kernel-characterisation-fat-cluster-fix.md) |
| 2026-06-11 | LANDED | [Short dense dispatch chains on calcite main (f2c8615): BIfNEL chain conversion threshold 6→2 on the flat-array path; ops/tick 700→678, BIfNEL 178→137/tick; same-day web A/B +4.8% t/s / −4.4% doomLoad (~+3–5% band — host ran 35% degraded, cross-day absolutes invalid); byte-identical dump, 300 tests, smoke 7/7](entries/2026-06-11-short-dense-dispatch-chains-calcite.md) |
| 2026-06-10 | LANDED | [Copy elimination landed on calcite main (967ddad+9ecc6de): copy propagation + DCE + store-forwarding over op streams; ops/tick 846→695 (−17.8%); web doom-all 3-run medians 70.5s/477.2K t/s/doomLoad 60.8s vs 75.0/456.0K/63.65 baseline; state-dump byte-identical, smoke 7/7, 300 tests](entries/2026-06-10-copy-elimination-landed-calcite.md) |
| 2026-06-10 | LANDED | [rep-generic MERGED to calcite main cc729b2: 341-line x86 string-op cheat deleted; merge-review fixes first (descriptor-carried IP/cycle commit names, key_property routing, descriptor-driven diagnostics — zero literal cabinet names in the rep path); verified pre-merge (288 tests, byte-identical A/B, smoke 7/7) and post-merge from main (288 tests, smoke 7/7, doom title @6M ticks)](entries/2026-06-10-rep-generic-merged-to-calcite-main.md) |
| 2026-06-09 | LANDED | [rep-generic VERIFIED: five layered recogniser/applier defects fixed (subtrahend capture, mirror-name routing, continuation gate, base scaling, flags commit); smoke 7/7, calcite-cli A/B vs main byte-identical on all 7 carts × 2M ticks, doom-all bench +0.5% wall / −1.5% t/s (inside noise); was BRANCH on feat/rep-generic 17fe7da — merged to calcite main 2026-06-10](entries/2026-06-09-rep-generic-verified-smoke-and-bench.md) |
| 2026-06-09 | FINDING | [doomLoad re-weighted by ticks (= calcite wall): EDR-DOS kernel 0x55 is ~49% of doomLoad, __I4D ~22% not 46%; cycle-weighting was the wrong metric; kernel file-I/O path is the untouched, cardinal-rule-free lever](entries/2026-06-09-doomload-tick-weighted-heatmap-kernel-is-the-lion.md) |
| 2026-06-09 | FINDING | [Per-tick op profile: 845 dispatched ops per guest instruction; LoadSlot 28% + LoadLit 8% — copy elimination is the biggest unlisted perf lever; item-5 (unchecked slots) was already done 2026-04-14; detail in calcite log 2026-06-09](entries/2026-06-09-per-tick-op-profile-845-ops-copy-movement.md) |
| 2026-06-08 | BRANCH | [rep-generic cheat-removal dispatcher recovered from wedged 2026-05-29 session (committed 247b274, pushed); deletes 341-line x86 string-op table, unit-green — but smoke 6/7 PANIC: recogniser's ip_extra_advance_slot models top-level Add(dispatch,prefixLen) while real cabinets encode _repContinue-gated IP−prefixLen body; not shippable, full analysis in calcite log 2026-06-08](entries/2026-06-08-rep-generic-dispatcher-recovered-ip-slot-gap.md) |
| 2026-05-28 | LANDED | [Keyboard branch 1.8× regression closed: apply-on-transition (calcite f4da585) writes gated slot at set_pseudo_class_active time, per-tick path deleted; web doom-loading 79.5s/432K t/s vs 77.1s/446K master baseline, within ~3%](entries/2026-05-28-keyboard-branch-regression-closed.md) |
| 2026-05-18 | SUPERSEDED | [Keyboard branch (feat/keyboard-pseudo-input) fails web bench: boots to Mode-13h title @~1fps, never advances in 600s; pseudo_pulse never dismisses title. — superseded by 2026-05-19 fix (bridge bench-watch-wipe + Stride modulo regression) + 2026-05-28 regression close; branch now reaches in-game and benches at 79.5s/432K t/s.](entries/2026-05-18-keyboard-branch-stuck-at-title-web-bench.md) |
| 2026-05-18 | FINDING | [Genericity↔perf cost BENCHED: feat/calcite-genericity 75.9s/448.5K t/s vs ef44f20-baseline 77-82s/416-443K — NO regression (measured, not from logbook); apply_input_edges drop is keyboard-branch not this one](entries/2026-05-18-genericity-perf-cost-isolated-no-per-tick-regression.md) |
| 2026-05-18 | LANDED | [calcite feat/retire-keyboard hand-partitioned into feat/keyboard-pseudo-input + feat/calcite-genericity; both build clean, now on origin](entries/2026-05-18-calcite-feat-retire-keyboard-bundle-hand-partitioned-into-tw.md) |
| 2026-05-15 | LANDED | [Branch reorg: calcite main reset to ef44f20 (old-kbd state), retire-keyboard/genericity parked on feat/retire-keyboard; cherry-pick disentangle abandoned (one ball)](entries/2026-05-15-branch-reorg-calcite-main-working-keyboard-state-retire-keyb.md) |
| 2026-05-14 | DEAD | [Item #5 unchecked slot access: -30% ticks/sec web bench, write-off; do not retry without a story for why result differs](entries/2026-05-14-item-5-unchecked-slot-access-attempt-30-ticks-sec-on-web-ben.md) |
| 2026-05-14 | PLAN | [First-principles re-framing: seven inefficiencies ranked, ~2500x headroom, directive to pick one mid-table item and do it properly](entries/2026-05-14-first-principles-re-framing-of-where-the-calcite-ceiling-liv-2.md) |
| 2026-05-12 | FINDING | [Per-dispatch-key phase-3 compile-cost wedge: 232x1.1s = 9x baseline, far over 1.5x gate; win is branch-free path shape; value-set dedup now mandatory](entries/2026-05-12-per-dispatch-key-specialisation-phase-3-wedge-compile-only-v.md) |
| 2026-05-12 | BRANCH | [Per-dispatch-key specialisation phase 2 (specialise_assignments partial evaluator, 18 tests) parked on feat/calcite-genericity, not on main](entries/2026-05-12-per-dispatch-key-specialisation-phase-2-landed-2.md) |
| 2026-05-12 | BRANCH | [Per-dispatch-key specialisation phase 1 (discover_hot_key/rank, 10 tests, --opcode auto-picked) parked on feat/calcite-genericity, not on main](entries/2026-05-12-per-dispatch-key-specialisation-phase-1-landed-3.md) |
| 2026-05-12 | BRANCH | [probe-specialise validated per-opcode collapse 10-200x on hot 178 assignments; six-phase plan filed; probe parked on feat/calcite-genericity, not on main](entries/2026-05-12-probe-specialise-per-dispatch-key-specialisation-validated-p-4.md) |
| 2026-05-12 | PLAN | [Idea: per-opcode specialisation as the architectural move structurally upstream of all other perf passes; see 2026-05-12 per-dispatch-key plan](entries/2026-05-12-idea-per-opcode-specialisation-the-architectural-move-5.md) |
| 2026-05-12 | DEAD | [Identity-branch pruning built but broke doom8088 boot; downstream pass keys on dispatch-key set; not on main](entries/2026-05-12-identity-branch-pruning-tried-broke-doom8088-parked-6.md) |
| 2026-05-12 | PLAN | [Filed __I4D whole-routine semantic substitution plan (compile-time region recogniser + symbolic evaluator); six phases, no env gate](entries/2026-05-12-plan-filed-i4d-whole-routine-semantic-substitution-7.md) |
| 2026-05-11 | LANDED | [Release landing page web/site/index.html: 4-step DOS-EDIT wizard wrapping build.js; verified end-to-end in web preview](entries/2026-05-11-release-oriented-landing-page-index-html.md) |
| 2026-05-11 | SUPERSEDED | [doomLoad cycle-weighted heatmap: __I4D 46.1% of cycles — interpretation corrected 2026-06-09: by ticks (= wall) kernel 0x55 is 48.6% and __I4D 22.3%; raw data + segment resolution still valid](entries/2026-05-11-doomload-cycle-weighted-heatmap-i4d-32-bit-divide-is-the-lio-2.md) |
| 2026-05-11 | FINDING | [FPS baseline reframed: use fuzzy ~1-2 fps noisy band not 1.70; added bench-host hygiene rule; no code changed](entries/2026-05-11-fps-baseline-reframed-as-fuzzy-1-2-fps-bench-host-hygiene-ru-3.md) |
| 2026-05-08 | FINDING | [BIF2 fusion isolated on web bench: real win +4.5% throughput / +8% fps; the 2026-05-07 +47% was a debunked CLI-bench artefact](entries/2026-05-08-bif2-fusion-isolated-4-5-throughput-8-in-game-fps.md) |
| 2026-05-08 | FINDING | [Canonical bench set fixed at three profiles; 2026-05-08 old-kbd baseline 77.1s engine-run, doomLoad 84.9% of wall; CALCITE_REPO harness fix](entries/2026-05-08-canonical-bench-set-2026-05-08-baseline-old-kbd-branch-2.md) |
| 2026-05-07 | FINDING | [apply_input_edges regression (a5e8eee) fixed by 6d9e80a, 162K->297K ticks/sec; both commits on a branch, NOT on current main ef44f20](entries/2026-05-07-apply-input-edges-regression-fixed-recovered-1-83-bif2-fusio.md) |
| 2026-05-07 | FINDING | [BIF2 +47.3%/-31.8% wall DEBUNKED: this was the CLI-bench artefact against a regressed 142K baseline; real web win is +4-8%](entries/2026-05-07-pre-ship-fps-lead-calcite-bif2-fuse-1-is-a-31-8-win-on-doom-2.md) |
| 2026-05-07 | DEAD | [Pre-ship FPS lead #1 widen fuse_loadstate_branch: zero new candidates, reverted; 97.3% BIfNEL predecessors are Jump](entries/2026-05-07-pre-ship-fps-lead-1-widen-fuse-loadstate-branch-retired-3.md) |
| 2026-05-07 | FINDING | [Pre-ship FPS survey: compile.rs 96% fusion claim false, real hit-rate 0.8%; op mix flat across stages; five leads filed](entries/2026-05-07-pre-ship-fps-bottleneck-survey-4.md) |
| 2026-05-07 | BRANCH | [rep_fast_forward genericity phase 3a (CMPS/SCAS recogniser + BulkClass, 15 tests) parked on feat/retire-keyboard, not on main](entries/2026-05-07-rep-fast-forward-genericity-mission-phase-3a-landed-5.md) |
| 2026-05-07 | BRANCH | [rep_fast_forward genericity phase 2 (CALCITE_REP_GENERIC validator, virtual_regions) parked on feat/retire-keyboard, not on main](entries/2026-05-07-rep-fast-forward-genericity-mission-phase-2-landed-6.md) |
| 2026-05-06 | BRANCH | [rep_fast_forward genericity phase 1 (structural recogniser + descriptors, 9 tests) parked on feat/retire-keyboard, not on main](entries/2026-05-06-rep-fast-forward-genericity-mission-phase-1-landed.md) |
| 2026-05-06 | PLAN | [Filed rep_fast_forward genericity mission plan: five checkpoints, recogniser must not read slot-name chars, perf gate +-1% web+CLI](entries/2026-05-06-plan-filed-rep-fast-forward-genericity-mission-2.md) |
| 2026-05-06 | BRANCH | [Keyboard retirement Phase B (pseudo_pulse, --press-events, set_keyboard deleted) parked on feat/retire-keyboard; main still has set_keyboard](entries/2026-05-06-phase-b-finished-bench-harness-migrated-set-keyboard-retired-3.md) |
| 2026-05-05 | BRANCH | [calcite-genericity audit tasks 1-4 (delete cdff, debug-summary + pc-video crates, comment sweep) parked on feat/calcite-genericity; cdff still live on main](entries/2026-05-05-calcite-genericity-cleanup-tasks-1-4-of-the-audit-list.md) |
| 2026-05-05 | LANDED | [Keyboard full PC layout: kiln KEYBOARD_KEYS gains Ctrl/Shift/Caps/Del/F1-F10 (56 rules), calcite.html 6x11 responsive grid; smoke 7/7](entries/2026-05-05-keyboard-full-pc-layout-esc-f1-f10-ctrl-shift-caps-responsiv-2.md) |
| 2026-05-05 | BRANCH | [Plan + proof for JS-free keyboard via :active; raw-Chrome propagation proven 11/11; calcite recogniser fix parked on feat/retire-keyboard, not on main](entries/2026-05-05-plan-initial-cleanup-js-free-keyboard-via-active-3.md) |
| 2026-05-02 | LANDED | [kbdtap: setvar_pulse=keyboard,VALUE,HOLD action + cond:repeat sustain fix; CLI doom-loading bench reaches in-game at 34.65M ticks](entries/2026-05-02-kbdtap-bench-reaches-in-game.md) |
| 2026-05-01 | LANDED | [Big repo cleanup: script-primitive layer (stride/burst/cond/halt), tests/bench harness, web/player merge; web bench 143s/34.3M post-merge](entries/2026-05-01-repo-cleanup-script-primitives-bench-harness-web-player-merg.md) |
| 2026-05-01 | LANDED | [Keyboard latch fix: --kbdScancodeLatch holds scancode level-readable until next edge (port 0x60); fixed zork double-press, doom held-arrow](entries/2026-05-01-keyboard-latch-port-0x60-holds-break-code-until-isr-services-2.md) |
| 2026-05-01 | LANDED | [LoadPackedByte euclid to bitwise byte extract (calcite-engine work, detail in calcite log)](entries/2026-05-01-loadpackedbyte-euclid-bitwise-byte-extract-3.md) |
| 2026-04-30 | FINDING | [FxHashMap swap: +25% ingame fps, -24% web level-load (calcite-engine work, detail in calcite log)](entries/2026-04-30-fxhashmap-swap-25-ingame-fps-24-web-level-load.md) |
| 2026-04-30 | FINDING | [Web flamegraph: exec_ops dominates, hashing is 17% of time (calcite-engine work, detail in calcite log)](entries/2026-04-30-web-flamegraph-exec-ops-dominates-hashing-is-17-2.md) |
| 2026-04-30 | DEAD | [read_mem borrow-overhead fix tried, dead lead, reverted (calcite-engine work, detail in calcite log)](entries/2026-04-30-read-mem-borrow-overhead-fix-dead-lead-reverted-3.md) |
| 2026-04-30 | DEAD | [BIfNEL2 fusion dead lead, left off by default (calcite-engine work, detail in calcite log)](entries/2026-04-30-bifnel2-fusion-dead-lead-off-by-default-4.md) |
| 2026-04-29 | FINDING | [Runtime op-adjacency profile post-fusion truth (calcite-engine work, detail in calcite log)](entries/2026-04-29-runtime-op-adjacency-profile-post-fusion-truth.md) |
| 2026-04-29 | DEAD | [REP FFD: leave alone (calcite-engine work, detail in calcite log)](entries/2026-04-29-rep-ffd-leave-alone-2.md) |
| 2026-04-29 | LANDED | [calcite DiskWindow to WindowedByteArray rename (calcite-engine work, detail in calcite log)](entries/2026-04-29-calcite-diskwindow-windowedbytearray-rename-3.md) |
| 2026-04-29 | DEAD | [load+compare+branch widening dead lead, reverted (calcite-engine work, detail in calcite log)](entries/2026-04-29-load-compare-branch-widening-dead-lead-reverted-4.md) |
| 2026-04-29 | DEAD | [fusion FFD funnel data + verdict: dead end on this window (calcite-engine work, detail in calcite log)](entries/2026-04-29-fusion-ffd-funnel-data-verdict-dead-end-on-this-window-5.md) |
| 2026-04-29 | FINDING | [fusion FFD framing + diag redesign (calcite-engine work, detail in calcite log)](entries/2026-04-29-fusion-ffd-framing-diag-redesign-6.md) |
| 2026-04-29 | LANDED | [bridge v41: hash-gated emit + 30Hz sampler; paintFps 19.5->2.1, vramFps 1.6 true visible rate](entries/2026-04-29-bridge-hash-gated-emit-30hz-sampler-7.md) |
| 2026-04-29 | DEAD | [fusion disabled by default, net loss, investigation pending (calcite-engine work, detail in calcite log)](entries/2026-04-29-fusion-disabled-by-default-net-loss-investigation-pending-8.md) |
| 2026-04-29 | FINDING | [fusion-sim: 88.6% body compose on doom column-drawer (calcite-engine work, detail in calcite log)](entries/2026-04-29-fusion-sim-88-6-body-compose-on-doom-column-drawer-9.md) |
| 2026-04-29 | LANDED | [calcite-v2-rewrite Phase 1 lands (calcite-engine work, detail in calcite log)](entries/2026-04-29-calcite-v2-rewrite-phase-1-lands-10.md) |
| 2026-04-28 | LANDED | [calcite Phase 3 prototype: closure backend (calcite-engine work, detail in calcite log)](entries/2026-04-28-calcite-phase-3-prototype-closure-backend.md) |
| 2026-04-28 | LANDED | [calcite Phase 2: recogniser substrate (calcite-engine work, detail in calcite log)](entries/2026-04-28-calcite-phase-2-recogniser-substrate-2.md) |
| 2026-04-28 | LANDED | [Load-time fusion: byte_period + fusion_sim + body-composition probe (calcite-engine work, detail in calcite log)](entries/2026-04-28-load-time-fusion-byte-period-fusion-sim-3.md) |
| 2026-04-28 | DEAD | [Replicated-body recogniser built, dead lead (calcite-engine work, detail in calcite log)](entries/2026-04-28-replicated-body-recogniser-built-dead-lead-4.md) |
| 2026-04-28 | LANDED | [XLAT segment-override fix in kiln: --directSeg fixes doom SS:BX colormap; smoke 7/7, doom in-game web](entries/2026-04-28-xlat-segment-override-fix-kiln-correctness-5.md) |
| 2026-04-28 | LANDED | [3 word-slot memory write scheme (6 byte-slots to 3 word-slots): doom8088 -7.3% size, -6.2% ingame](entries/2026-04-28-3-word-slot-scheme-6.md) |
