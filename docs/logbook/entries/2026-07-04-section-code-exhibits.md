# 2026-07-04 — Six new verbatim code exhibits across the How-it-works sections

Owner ask ("enrich with extra snippets — quirks, horrible functions").
All shapes verified against the kiln emitters (not quoted from prose):
CPU gains a "How a program decides anything" fold — JZ's IP row
(branchless: --bit(flags,6) × signed distance, control.mjs) + JL's
sign≠overflow condition as a+b−2ab XOR (cross-links util). Keys gains
the debug read-out — registers printed via counter-reset/counter()
because CSS can't display an integer (template.mjs emitDebugDisplay).
Screen gains the real per-pixel rule (#p31840, cell 343600) and the
--paletteRGB arm with live DAC reads ×255/63 (6-bit VGA DAC;
pixels.mjs) + the verified fact it's the file's only <color>-returning
function. Disk gains window byte 48's full arm — LBA dug out of cell
632 (emit-css.mjs readMem window). Clock gains the 1978 cycle price
list (NOP 3 / MOV 2-or-9 / AAM 83, cycle-counts.mjs). Memr pays off
the keyboard-bridge prose with the 1280/1281 arms. Playwright: all
six render, 0 console errors; vite build green. Owner prose review
pending per ABOUT-SCRIPT.md.
